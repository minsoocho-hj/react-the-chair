import React from 'react';
import styles from './fileUploader.module.css';

const FileUploader = ({ onFileChange }) => {
	return <input type='file' className={styles.uploader} onS />;
};

export default FileUploader;
